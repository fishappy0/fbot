import { NextPage } from 'next'

const missing_page: NextPage = () => {
    return(
        <div className="placeholder">
            404 - Is this page even exist ? why 404 means missing but there's a page here ? are we even... real? is this the matrix?????
        </div>
    )
}

export default missing_page